- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gvimrc:
    ~/.vimrc:
    ~/.tmux.conf:
    ~/.bashrc:
    ~/.ghci:
    ~/.ideavimrc:
    ~/.config/:
      path: config/*/*
      create: true
      glob: true

